{
  "name": "art-archive",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "stack": "netlify dev",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "build:app": "VITE_API_BASE=https://ptrart.netlify.app vite build && node tools/sync-android.mjs",
    "apk": "npm run build:app && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@netlify/blobs": "^10.7.12",
    "ag-psd": "^25.0.0",
    "fflate": "^0.8.3",
    "gsap": "^3.12.5",
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "vite": "^5.4.11",
    "vitest": "^2.1.8"
  }
}
